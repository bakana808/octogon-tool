
query Entrant($id: ID!) {
  entrant(id: $id) {
    id
    participants {
      player {
        id
        gamerTag
        name
      }
    }
    standing {
      stats {
        score {
          value
        }
      }
    }
  }
}
